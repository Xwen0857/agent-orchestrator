# Primary Requirement Example

This file is a format example only.

Operational primary records are written outside the repository, by default to:

- `~/.openclaw-state/agent-orchestrator/planner/primary.md`

If `AGENT_ORCHESTRATOR_STATE_DIR` is set to an absolute path, the runtime primary file is written to:

- `$AGENT_ORCHESTRATOR_STATE_DIR/planner/primary.md`

| primary_id | title | scope | constraints | acceptance_criteria | priority | status | start_signal |
|---|---|---|---|---|---|---|---|
| primary_example_001 | Example requirement | Example scope | Do not modify external systems | Example acceptance criteria | P1 | DRAFT | NO |
