# Detection Heuristics

## 注入风险最小判定
- 命中 shell 注入模式：`;`, `&&`, `||`, `` ` ``, `$(` 与高危命令组合。
- 命中 prompt 注入模式：要求忽略系统规则、绕过审批、泄露密钥。

## 未知执行最小判定
- 不在允许命令前缀清单的 shell 命令。
- 不在已注册模块列表的动态模块调用。

## 升级规则
- Tier 3 在 60 分钟内连续命中 >= 3 次，升级为 Tier 2。
